# Navigation and contact fixes

Fixed:
- OUR DOCTORS now points to `doctors.php`, which exists and loads active doctors from the database.
- CONTACT US now points to a new `contact.php`.
- All public HOME/logo links now point to `index.php`.
- Kept `main.html` as a compatibility redirect to `index.php`.
- "Place a call through today" is now a clickable `tel:` link.
- "Chat with us today" is now a WhatsApp link plus email link.
- BOOK AN APPOINTMENT continues to `patientlogin.php`, because the original application requires patient authentication before booking.
- Added the same working navigation to the new doctors/contact pages.

Note: WhatsApp links require WhatsApp/the browser to be available on the device. The phone link requires a device/app that can place calls.
